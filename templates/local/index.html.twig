{% extends 'base.html.twig' %}

{% block page_title %}{{ 'local.title_list'|trans }}{% endblock %}

{% block page_content %}
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">{{ 'local.title_list'|trans }}</h1>
                </div><!-- /.col -->
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Home</a></li>
                        <li class="breadcrumb-item active">{{ 'local.title_list'|trans }}</li>
                    </ol>
                </div><!-- /.col -->
            </div><!-- /.row -->
        </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->
    <div class="card">
        <div class="card-header text-right">
            <a class="btn-sm btn btn-success" href="{{ path('local_new') }}"><i class="fa fa-plus"></i>
                {{ 'button.add'|trans({}, 'messages') }}</a>
        </div>
        <!-- /.card-header -->
        <div class="card-body table-responsive">
            <div class="table-striped table-head-fixed text-nowrap" id="locals">{{ 'loading'|trans({}, 'messages') }}</div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="application/javascript">
        $(function () {
            $('#locals').initDataTables({{ datatable_settings(datatable) }});
        });
    </script>
    {% include 'local/javascript.html.twig' %}
{% endblock %}